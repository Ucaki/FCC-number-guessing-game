
# Student Database Project

Scripts to insert and query student data in a PostgreSQL database.

- `courses.csv`, `students.csv` – Sample data
- `db.sql` – Database setup
- `insert_data.sh` – Script to insert CSV data into the database
- `student_info_Otherepo.sh` – Query examples
