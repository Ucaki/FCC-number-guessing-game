
# World cup DB scripts

Contains Bash scripts to insert and query World Cup data into a PostgreSQL database.  

Files:
- `insert_data.sh` – Inserts CSV data into `teams` and `games` tables.
- `queries.sh` – Example SQL queries to extract statistics.
- `games.csv` – Sample dataset for the project.
